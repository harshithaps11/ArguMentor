# ArguMentor Node.js Backend

Handles user management, authentication, and general data for ArguMentor.

## 🚀 Getting Started

### 1. Install dependencies

```bash
npm install
```

### 2. Run the server

```bash
npm run dev
# or
npm start
```

- Server runs at `http://localhost:5000/` by default (see `server.js`).

## 📚 Endpoints

- `/api/users` - User management
- `/api/auth` - Authentication
- ... (add more as needed)

## 🛠️ Notes

- This backend is independent from the FastAPI backend.
- Use for user data, sessions, and other non-AI features. 